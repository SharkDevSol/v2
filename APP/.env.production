# API Configuration
# In production, the API URL is auto-detected from the browser domain
# (e.g., https://v2.skoolific.com or https://iqrab3.skoolific.com)
# No need to change this file per deployment

# Production Mode
NODE_ENV=production
