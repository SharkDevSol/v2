/**
 * LanguageSelector Component Export
 * 
 * Provides language selection functionality with dropdown and button group variants.
 * Supports English, Amharic, and Arabic with RTL layout for Arabic.
 */

export { default } from './LanguageSelector';
export { default as LanguageSelector } from './LanguageSelector';
